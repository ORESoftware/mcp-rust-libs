# Rust MCP fleet audit — August 3, 2026

Tracking: DEN-161, DEN-779, DEN-852, DEN-957, DEN-959, DEN-962, DEN-965, DEN-1248, and DEN-1257.

## Exact live coverage

- **24 of 24** visible standalone Rust MCP server repositories have an open hardening pull request.
- **0** existing visible servers remain inventory-only without a PR-backed audit lane.
- **5** visible organization server repositories remain missing and are tracked by DEN-162 through DEN-166.
- The canonical shared hardening library and fleet auditor are under review in `ORESoftware/mcp-rust-libs#1`.

The matching machine-readable record is `fleet/audit-2026-08-03.json`. An open PR is not the same as merged, green, or compliant; each row records its current review or blocker state.

## Hardening PR ledger

| Repository | PR | Current state |
| --- | ---: | --- |
| `3FA-app/3FA-mcp-server.rs` | #13 | existing hardening lane |
| `messaging-intel/msgint-mcp-server.rs` | #9 | focused privacy/conformance lane; Actions fail before step allocation |
| `athlet-o/athleto-mcp-server.rs` | #5 | existing hardening lane |
| `canonical-cloud/canonical-mcp-server.rs` | #16 | existing hardening lane |
| `akrion-sim/akrion-mcp-server.rs` | #5 | existing hardening lane |
| `benefactor-cc/benefactor-cc-mcp-server.rs` | #14 | green existing lane; residual process/proxy findings recorded |
| `claritas-viz/claritas-viz-mcp-server.rs` | #5 | green and review-ready bounded subprocess lane |
| `anticaptrad/act-mcp-server.rs` | #5 | green and review-ready custom stable-lifecycle HTTP compatibility lane |
| `fiducia-cloud/fiducia-mcp-server.rs` | #10 | existing hardening lane |
| `daedalus-fab/daedalus-fab-mcp-server.rs` | #7 | existing hardening lane |
| `declarative-migrations/declarative-migrations-mcp-server.rs` | #5 | existing hardening lane |
| `sagitta-stack/sagitta-mcp-server.rs` | #14 | existing hardening lane |
| `scintilla-run/scintilla-mcp-server.rs` | #13 | existing hardening lane |
| `sonus-auris/sonus-auris-mcp-server.rs` | #8 | green existing lane; residual process/proxy findings recorded |
| `rust-ssr-demos/rust-ssr-mcp-server.rs` | #5 | focused real-process HTTP lane; Actions fail before step allocation |
| `shared-auth/shared-auth-mcp-server.rs` | #12 | focused exact-host/no-proxy follow-up; source materializer queued |
| `quaestor-ledger/quaestor-ledger-mcp-server.rs` | #6 | green existing lane; residual process/proxy findings recorded |
| `usa-acc/usa-acc-mcp-server.rs` | #5 | existing hardening lane |
| `file-tunnel/ftnl-mcp-server.rs` | #1 | focused bounded subprocess lane |
| `discrete-event-systems/des-mcp-server.rs` | #11 | existing hardening lane |
| `StreemPilot/streempilot-mcp-server.rs` | #2 | green and review-ready exact `rmcp =3.1.0` migration |
| `hypesiege/hypesiege-mcp-server.rs` | #2 | exact `rmcp =3.1.0` and tracked lockfile committed; permanent workflow approval required |
| `drone-mngr/drone-mngr-mcp-server.rs` | #1 | focused physical-control lane; workflow approval required |
| `drone-mngr/laser-ptr-ctrl-mcp-server.rs` | #1 | focused physical-control lane; workflow approval required |

## Fleet findings enforced by the shared auditor

The audit ratchet detects:

- handwritten JSON-RPC and hard-coded obsolete protocol revisions;
- pre-1.0 `rmcp` compatibility debt;
- Streamable HTTP below the `rmcp` 1.4.0 DNS-rebinding security floor unless an equivalent reviewed Host-validation patch is documented;
- `wait_with_output`, `Command::output`, and whole-body reqwest sinks;
- stdout writes in production Rust;
- credentialed HTTP clients without redirect denial, exact-host policy, or `no_proxy`;
- Streamable HTTP without an obvious bearer or Host boundary;
- permissive tool input structs without closed runtime schemas;
- mutating tools without an independent runtime gate, target-bound confirmation, idempotency, or dry-run signal;
- serialized MCP results without an obvious final output ceiling;
- mutable GitHub Action pins, implicit workflow permissions, and persisted checkout credentials;
- missing lockfiles, CI, tests, and real-binary initialize/tools lifecycle tests.

A separate deployable-application ratchet now requires `Cargo.lock` to exist, be valid, be tracked by Git, and not be ignored. Its adversarial fixtures cover missing, ignored, untracked, invalid, empty, and valid lockfiles, while library-only packages remain outside the application policy.

Static findings are review signals, not proof. False positives require a repository-local, evidence-backed exception instead of weakening the fleet rule globally.

## SDK and protocol baseline

The current signed upstream `rmcp` release is **3.1.0**. Fleet policy is lifecycle- and dependency-state-aware rather than “latest major equals secure”:

- new SDK migrations normally pin an exact reviewed release, currently `=3.1.0`, and commit the matching lockfile;
- reviewed 2.2.x stdio servers may remain on stable MCP `2025-11-25` until a breaking lifecycle migration is justified;
- Streamable HTTP must meet the known 1.4.0 security floor or carry an equivalent reviewed Host-validation patch;
- custom compatibility endpoints must be explicitly bounded, tested, and tracked as exceptions;
- manifest ranges must not silently advance a reviewed server to a new SDK or macro version.

StreemPilot now proves exact manifest/lock resolution and passed CI run `30834492360`. HypeSiege’s substantive compile, Clippy, unit, stdio, docs, and release checks passed before publication exposed that `Cargo.lock` was ignored; the lockfile is now tracked and the one-shot materializer has been removed. Its remaining gate is authorized permanent-workflow execution.

## Physical-control standard

Drone and laser MCP servers require a stricter boundary than ordinary diagnostics:

1. The MCP process cannot replace downstream authorization, geofences, hardware interlocks, or human supervision.
2. Every credentialed upstream origin is exact-host allowlisted; redirects and ambient proxies are disabled.
3. Remote cleartext service networks require a separate explicit override and may never bypass the allowlist.
4. Mutations default off and require confirmations bound to the selected resource and action.
5. Runtime validation enforces finite coordinates, route and duration limits, UUID form, free-form JSON ceilings, and final output ceilings.
6. Real-binary lifecycle tests prove initialization, tool discovery, unknown-field rejection, default-disabled mutations, and secret-free diagnostics.
7. CI ratchets these invariants with immutable actions and read-only default permissions.

## Remaining execution order

1. Complete Shared Auth PR #12 source materialization, remove its temporary workflows, and require permanent CI before review readiness.
2. Restore Actions step allocation in Messaging Intel and Rust SSR without weakening their privacy, policy, or audit jobs.
3. Approve HypeSiege, drone, laser, and shared-library workflows and require real remote evidence.
4. Land `mcp-rust-libs#1`, cut an immutable reviewed release, and replace repository-local process/HTTP/testkit forks.
5. Resolve residual process/proxy findings on Benefactor, Sonus, Quaestor, and any other existing green lane before calling the fleet complete.
6. Create the five missing organization MCP repositories from the hardened template and wire them into real monorepos through an approved repository-creation capability.
7. Generate this fleet report in CI from exact checked-out revisions and reject stale PR-state rows.
